(REPRODUCER for an OPEN offline-chain defect: motion AFTER g69.)
(
(The machine runs this correctly. The offline preview/sim chain does not:
(the payload contains a phantom ~600-900 mm XYZ excursion on the trailing
(move, which the real machine never makes. Measured 2026-08-28 with
(sim_parity: truth->sim 0.001 (the sim covers the real path) but
(sim->truth 897 (the sim invents a move).
(
(No previously shipped program exercised this: twp_simple_example ships its
(final g69 COMMENTED OUT, and parity_linear's g69 is the first line with no
(motion before it. This program is deliberately the minimal case - it has NO
(table move, so the table-aware composition never engages and cannot be
(implicated (verified: the divergence is identical with the composition
(stubbed off).
(
(Reproduce:
(  scripts/sim_parity.py gate --corpus scripts/parity_corpus/_open_g69_tail.json
(Expected: FAIL until the g69-tail defect is fixed.
g69
g10 l2 p0 x1300 y-200 z-1400 a0
m6 t3 g43 h3
g0 x0y0z100 a0
g68.2 x50 y50 z-50 q121 i30 j15
g53.3 x0y0z100
o<square> call
g69
g0 a0
M2
