# Historical regression fixtures

These retired profiles and models are kept only to reproduce recorded tests
and kinematics regressions. They are not installed or offered as examples.
Do not launch them for new acceptance runs. Use the three profiles in
`examples/sim_config/profiles.json` instead. The 55-degree recordings retain
their original geometry; the active 6-axis example uses the 45-degree gantry.
