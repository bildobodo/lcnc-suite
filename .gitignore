__pycache__/
*.pyc
*.pyo
*.swp
.DS_Store
.idea/

.venv/
venv/

*.log
*.tmp

# Machine-specific runtime configs (not portable)
lcnc-gateway/settings.json
lcnc-gateway/tool_library.json

# If you have generated artifacts:
dist/
build/

# Claude Code local config (may contain allowed-commands referencing private repos)
.claude/settings.local.json

# Machine-specific / downloaded files
rs274ngc.var*

# Local-only DMU 160 P example (not shipped: the upstream STL source repo
# declares no license, so redistribution isn't clearly granted — see
# Sigma1912/LinuxCNC_Demo_Configs; his GPL-3.0 vtk-vismach is credited in
# NOTICE as the kinematic reference). Kept locally for testing.
examples/sim_config/machine-dmu160p/
examples/sim_config/lcnc_suite_sim_dmu160p.ini
lcnc-webui/src/viewer/machineDmu160p.test.ts
*.dsc
runlogs/

node_modules/
runlogs/
test-results/
playwright-report/

# Untracked working docs (swept in by an over-broad git add during WS-D; user-decided untracked)
ARCHITECTURE_HANDOFF.md
ISSUE_35_HANDOFF.md
PERFORMANCE_MODULARIZATION_HANDOFF.md
examples/sim_config/probe-results-interptest.txt
